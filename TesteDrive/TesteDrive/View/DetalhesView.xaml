<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TesteDrive.View.DetalhesView"
             Title="{Binding Veiculo.Modelo}">

    <StackLayout Padding="15">
        <TableView Intent="Settings">
            <TableRoot>
                <TableSection Title="Acessórios">
                    <SwitchCell Text="{Binding InfoFreioAbs}" On="{Binding TemFreioAbs, Mode=TwoWay}"/>
                    <SwitchCell Text="{Binding InfoArCondicionado}" On="{Binding TemArCondicionado, Mode=TwoWay}"/>
                    <SwitchCell Text="{Binding InfoTravaEletrica}" On="{Binding TemTravaEletrica, Mode=TwoWay}"/>
                    <!--<TextCell Text="{Binding InfoValorTotal, Mode=TwoWay}"/>-->
                </TableSection>
            </TableRoot>
        </TableView>


        <Label Text="{Binding InfoValorTotal, Mode=TwoWay}" HorizontalOptions="End"/>
        <Button x:Name="btProximo" Text="Próximo" Clicked="btProximo_Clicked" VerticalOptions="End"/>
    </StackLayout>    
</ContentPage>