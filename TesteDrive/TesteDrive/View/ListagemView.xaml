<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TesteDrive.View.ListagemView"
             Title="Modelos">


    <ListView x:Name="listaVeiculos" ItemsSource="{Binding Veiculos}" Margin="15"  ItemTapped="listaVeiculos_ItemTapped">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <ViewCell.View>
                        <StackLayout Orientation="Horizontal" VerticalOptions="Center">
                            <Label Text="{Binding Modelo}" FontSize="20" VerticalTextAlignment="Center"/>
                            <Label Text=" - "/>
                            <Label Text="{Binding ValorTratado}" FontSize="15" FontAttributes="Bold" VerticalTextAlignment="Center"/>
                        </StackLayout>
                    </ViewCell.View>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>                
    </ListView>

</ContentPage>
